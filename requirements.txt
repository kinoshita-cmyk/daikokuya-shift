# 大黒屋シフト管理システム - Python 依存パッケージ
# Streamlit Cloud がこのファイルを読んで自動でインストールします
#
# 方針: 2026-07-10 まで正常稼働していた実績バージョンに固定する。
# バージョンを上げる場合は、必ずテスト環境で動作確認してから反映すること。

streamlit==1.50.0
ortools==9.15.6755
openpyxl==3.1.5
anthropic==0.97.0
reportlab==4.4.6
japanize-matplotlib==1.1.3
matplotlib==3.11.0
pandas==2.3.3
numpy==2.0.2
requests>=2.31,<3
streamlit-aggrid==1.2.1.post2

# --- 障害対応 (2026-07-15) ---
# pyarrow 25.0.0 (2026-07-10 リリース) に mimalloc 起因の Segmentation fault
# バグがあり、Streamlit Cloud 全体でアプリが起動不能になる障害が発生
# (Streamlit 公式フォーラムでスタッフ確認済み)。
# streamlit が起動時に読み込む pyarrow / protobuf は、勝手に最新版へ
# 更新されないようここで明示的に固定する。
# pyarrow の修正版リリース後も、動作確認してから上げること。
pyarrow==24.0.0
protobuf==6.33.6
